# MySQL/MariaDB Language Configuration for PolyGen
# Generates schema-wide DDL for @datasource("mysql") tables.

extension = ".sql"

[templates]
main = "mysql_file.ptpl"
main_output = "schema.sql"
main_per_file = false

[rhai]
prelude = ["rhai_utils/type_mapping.rhai"]
