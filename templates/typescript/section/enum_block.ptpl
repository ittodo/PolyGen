%-- TypeScript enum block. Context: enum (EnumDef)
export enum {{enum.name}} {
%for item in enum.items
%if item.is_comment
    // {{item.as_comment}}
%elif item.is_member
%if item.as_member.has_value
    {{item.as_member.name}} = {{item.as_member.value}},
%else
    {{item.as_member.name}},
%endif
%endif
%endfor
}
