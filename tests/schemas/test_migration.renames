# Test migration renames file

# Table rename (namespace.OldTable -> NewTable)
game.Player -> User;

# Field renames (namespace.Table.old_field -> new_field)
game.User.user_name -> name;
game.User.hp -> health;
