[package]
name = "PolyGen"
version = "0.1.0"
edition = "2021"

[dependencies]
pest = "2.7"
pest_derive = "2.7"
thiserror = "1.0"
heck = "0.5"
serde = { version = "1.0", features = ["derive"] } # Keep serde as it's used by csharp_model
askama = "0.12" # Keep askama
anyhow = "1.0.79" # Keep anyhow as it's a common error handling crate