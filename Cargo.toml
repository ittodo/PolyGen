[package]
name = "PolyGen"
version = "0.1.0"
edition = "2021"

[dependencies]
pest = "2.7"
pest_derive = "2.7"
thiserror = "1.0"
heck = "0.5"

askama = "0.12" # Keep askama
anyhow = "1.0.79" # Keep anyhow as it's a common error handling crate
