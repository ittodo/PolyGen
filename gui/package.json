{
  "name": "polygen-gui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cargo build --release --manifest-path ../Cargo.toml && vite",
    "build": "cargo build --release --manifest-path ../Cargo.toml && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:dev": "cargo build --release --manifest-path ../Cargo.toml && tauri dev",
    "tauri:build": "cargo build --release --manifest-path ../Cargo.toml && tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-dialog": "^2.6.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "@tauri-apps/cli": "^2.0.0",
    "svelte": "^5.0.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
